import { defineTheme } from 'pinceau'

export default defineTheme({
  alpine: {
    body: {
      backgroundColor: {
        initial: '#FFFFFF1',
        dark: '#0c0c0d'
      },

      color: {
        initial: 'green',
        dark: '#e4e4e7'
      }
    }
  }
})