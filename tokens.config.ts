import { defineTheme } from 'pinceau'

export default defineTheme({
  alpine: {
    body: {
      color: {
        value: {
          initial: 'green',
          dark: 'red'
        }
      }
    }
  }
})
